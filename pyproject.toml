[build-system]
requires = ["setuptools>=80", "wheel", "Cython", "numpy", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "pydpc/_version.py"
fallback_version = "0.1.3"
